[package]
name = "peripherals-status"
version = "0.1.0"
edition = "2021"
description = "Read-only sysfs enumerator for the Omarchy Peripherals bar plugin"
license = "MIT"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
